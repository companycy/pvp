package server.pvp

/**
 * Created by bjcheny on 6/22/14.
 */
object EquipmentRatioUtil {
  // assuming 100 players are in map foo
  // then, get actual luck value by adding up all
  // then figure out how many equipments there will be in this 100 games
  // then divide between all players based on their luck value
  // player's luck value -> base_num(eg., 100->10000, 200->20000)
  // then ratio = random() * base_num
  // sort all 100 players' ratio decedent
}
